{
  "channels": {
    "slack": {
      "groupPolicy": "allowlist",
      "channels": {
        "REPLACE_WITH_SLACK_CHANNEL_ID": {
          "allow": true,
          "requireMention": false
        }
      }
    }
  },
  "hooks": {
    "enabled": true,
    "token": "${OPENCLAW_HOOK_TOKEN}",
    "path": "/hooks",
    "maxBodyBytes": 262144,
    "defaultSessionKey": "hook:jira",
    "allowRequestSessionKey": false,
    "allowedSessionKeyPrefixes": [
      "hook:"
    ],
    "mappings": [
      {
        "match": {
          "path": "jira-relay"
        },
        "action": "agent",
        "wakeMode": "now",
        "name": "Jira Relay",
        "sessionKey": "hook:jira:{{issue.key}}:{{relay.correlationId}}",
        "messageTemplate": "Jira 任务事件\n\n工单：<{{issue.issueUrl}}|{{issue.key}} - {{issue.summary}}>\n看板：<{{context.boardUrl}}|{{context.boardName}}>\n事件：{{event.name}}\n当前状态：{{issue.status}}\n上一个状态：{{event.previousStatus}}\n类型：{{issue.issueType}}\n优先级：{{issue.priority}}\n领域：{{event.serviceDomain}}\n执行模式：{{event.executionMode}}\n\n任务目标：{{context.taskGoal}}\nPlanner 输出：{{context.plannerOutput}}\n任务上下文：{{context.taskContext}}\n描述：{{context.description}}\n\n请依据当前 workspace 中的身份、记忆和已安装 skills 处理这条 Jira 任务；如果状态已经进入“执行中”且任务边界清晰，可直接执行允许范围内的动作，并在回复里给出结果、证据和是否需要人工验收。",
        "deliver": true,
        "channel": "slack",
        "to": "channel:REPLACE_WITH_SLACK_CHANNEL_ID"
      }
    ]
  }
}
